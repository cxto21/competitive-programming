#include <bits/stdc++.h>

using namespace std;

#define DBG(x) cout << #x << “ = ” << (x) << "\n"

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int n, counter;
  int a[10];

  cin >> n;
  for (int i = 0; i < 10; ++i) {
    cin >> a[i];
  }

  for (int i = 0; i < 10; ++i) {
    cout << a[i];
  }

}
