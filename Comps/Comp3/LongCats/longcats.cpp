#include <bits/stdc++.h>

using namespace std;

#define DBG(x) cout << #x << “ = ” << (x) << "\n"

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int x, counter = 0;

  cin >> x >> "\n";

  while (counter != 40) {

    if (k 1)

  }

}
