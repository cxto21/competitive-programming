#include <bits/stdc++.h>

using namespace std;

#define DBG(x) cout << #x << “ = ” << (x) << "\n"

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int n, max = 0, ret, aux;
  cin >> n;

  /*	a * b = n ;b = n/a ; */

  for (int i = 1; i <= n; ++i) {

    if ((i * == n)) {
      aux = (i + j) * 2;
      if (aux < ret) {
        ret = aux;
      }

    }
  }

  cout << ret << "\n";
}
